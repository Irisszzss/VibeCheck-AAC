# VibeCheck-AAC

Dynamic web application that fetches "vibes". fortunes, jokes, and secretds from a node.js backend API that features a responsive glassmorphism UI with interactive color-changing themes based on user interaction.
